<%= form_for current_record do |f| %>
  <fieldset>
    <p>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </p>
  </fieldset>

  <fieldset class="buttons">
    <%= f.submit "Save This Widget" %>
  </fieldset>
<% end %>
